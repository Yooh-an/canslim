# Data processing and analysis
# Tested on pandas 2.2 / NumPy 1.26. Avoid implicit pandas 3 / NumPy 2 upgrades until validated.
pandas>=2.2,<3.0
numpy>=1.26,<2.0
pyarrow>=16,<25

# API and web requests
requests>=2.32,<3.0
# 0.2.49 and older can get Yahoo Edge 429s while fetching cookie/crumb data.
yfinance>=0.2.66,<0.3
# Required by newer yfinance transport; keep explicit for reproducible installs.
curl_cffi>=0.7.0
cffi>=2.0.0

# Utilities
tqdm>=4.66,<5.0
python-dotenv>=1.0,<2.0

# Logging
colorlog>=6.9,<7.0

# Terminal UI
rich>=13.0,<15.0

# Visualization
matplotlib>=3.9,<4.0
seaborn>=0.13,<0.14
